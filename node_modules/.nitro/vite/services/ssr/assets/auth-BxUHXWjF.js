import { t as supabase } from "./client-BprIDH4T.js";
import { i as Button, n as useAuth, r as Input } from "./useAuth-BPCYffQM.js";
import { a as Label, i as TabsTrigger, n as TabsContent, r as TabsList, t as Tabs } from "./tabs-CNwdcvwF.js";
import { useEffect, useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { AudioLines, Zap } from "lucide-react";
//#region src/routes/auth.tsx?tsr-split=component
function AuthPage() {
	const navigate = useNavigate();
	const { user, loading, supabaseError, enterDemoMode } = useAuth();
	const [email, setEmail] = useState("");
	const [password, setPassword] = useState("");
	const [fullName, setFullName] = useState("");
	const [busy, setBusy] = useState(false);
	useEffect(() => {
		if (!loading && user) navigate({ to: "/" });
	}, [
		loading,
		user,
		navigate
	]);
	const signIn = async () => {
		setBusy(true);
		try {
			const { error } = await supabase.auth.signInWithPassword({
				email,
				password
			});
			if (error) {
				if (error.message.toLowerCase().includes("network") || error.message.toLowerCase().includes("fetch")) toast.error("Cannot reach Supabase. Use demo mode to explore the app.");
				else toast.error(error.message);
				return;
			}
			toast.success("Welcome back to EchoTrade");
			navigate({ to: "/" });
		} catch {
			toast.error("Connection failed. Try demo mode.");
		} finally {
			setBusy(false);
		}
	};
	const signUp = async () => {
		setBusy(true);
		try {
			const { error } = await supabase.auth.signUp({
				email,
				password,
				options: {
					data: { full_name: fullName },
					emailRedirectTo: window.location.origin
				}
			});
			if (error) {
				if (error.message.toLowerCase().includes("network") || error.message.toLowerCase().includes("fetch")) toast.error("Cannot reach Supabase. Use demo mode to explore the app.");
				else toast.error(error.message);
				return;
			}
			toast.success("Account created — check your email to confirm");
			navigate({ to: "/" });
		} catch {
			toast.error("Connection failed. Try demo mode.");
		} finally {
			setBusy(false);
		}
	};
	const handleDemoMode = () => {
		enterDemoMode();
		toast.success("Demo mode active — explore EchoTrade freely!");
		navigate({ to: "/" });
	};
	return /* @__PURE__ */ jsx("main", {
		className: "flex min-h-screen items-center justify-center px-4 py-12",
		children: /* @__PURE__ */ jsxs("div", {
			className: "panel w-full max-w-md p-6",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "flex items-center gap-2",
					children: [/* @__PURE__ */ jsx("span", {
						className: "glow-ring flex size-9 items-center justify-center rounded-lg bg-primary/15",
						children: /* @__PURE__ */ jsx(AudioLines, { className: "size-5 text-primary" })
					}), /* @__PURE__ */ jsxs("span", {
						className: "font-display text-xl font-bold",
						children: ["echo", /* @__PURE__ */ jsx("span", {
							className: "text-gradient",
							children: "trade"
						})]
					})]
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "font-display mt-5 text-2xl font-semibold",
					children: "Voice trading, NSE & BSE"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-1 text-sm text-muted-foreground",
					children: "One login, three linked broker accounts, all funds in rupees."
				}),
				supabaseError && /* @__PURE__ */ jsxs("div", {
					className: "mt-4 rounded-lg border border-yellow-500/30 bg-yellow-500/10 px-3 py-2 text-xs text-yellow-600 dark:text-yellow-400",
					children: [
						"⚠️ Cannot connect to Supabase backend. You can still use",
						" ",
						/* @__PURE__ */ jsx("strong", { children: "Demo Mode" }),
						" to explore the app locally."
					]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-5 rounded-xl border border-primary/30 bg-primary/5 p-4",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-2",
							children: [/* @__PURE__ */ jsx(Zap, { className: "size-4 text-primary" }), /* @__PURE__ */ jsx("span", {
								className: "text-sm font-semibold",
								children: "Demo Mode — no account needed"
							})]
						}),
						/* @__PURE__ */ jsx("p", {
							className: "mt-1 text-xs text-muted-foreground",
							children: "Explore live prices, voice commands in English, Hindi & Gujarati, charts and all features. No data is saved."
						}),
						/* @__PURE__ */ jsxs(Button, {
							className: "mt-3 w-full",
							onClick: handleDemoMode,
							children: [/* @__PURE__ */ jsx(Zap, { className: "mr-1.5 size-4" }), " Continue as Guest (Demo)"]
						})
					]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "my-5 flex items-center gap-3 text-[11px] uppercase tracking-[0.2em] text-muted-foreground",
					children: [
						/* @__PURE__ */ jsx("span", { className: "h-px flex-1 bg-border" }),
						" or sign in ",
						/* @__PURE__ */ jsx("span", { className: "h-px flex-1 bg-border" })
					]
				}),
				/* @__PURE__ */ jsxs(Tabs, {
					defaultValue: "signin",
					children: [
						/* @__PURE__ */ jsxs(TabsList, {
							className: "w-full",
							children: [/* @__PURE__ */ jsx(TabsTrigger, {
								value: "signin",
								className: "flex-1",
								children: "Sign in"
							}), /* @__PURE__ */ jsx(TabsTrigger, {
								value: "signup",
								className: "flex-1",
								children: "Create account"
							})]
						}),
						/* @__PURE__ */ jsxs(TabsContent, {
							value: "signin",
							className: "space-y-3 pt-4",
							children: [
								/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, {
									htmlFor: "email",
									children: "Email"
								}), /* @__PURE__ */ jsx(Input, {
									id: "email",
									type: "email",
									value: email,
									onChange: (e) => setEmail(e.target.value),
									onKeyDown: (e) => e.key === "Enter" && signIn()
								})] }),
								/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, {
									htmlFor: "password",
									children: "Password"
								}), /* @__PURE__ */ jsx(Input, {
									id: "password",
									type: "password",
									value: password,
									onChange: (e) => setPassword(e.target.value),
									onKeyDown: (e) => e.key === "Enter" && signIn()
								})] }),
								/* @__PURE__ */ jsx(Button, {
									className: "w-full",
									onClick: signIn,
									disabled: busy || !email || !password,
									children: busy ? "Signing in…" : "Sign in"
								})
							]
						}),
						/* @__PURE__ */ jsxs(TabsContent, {
							value: "signup",
							className: "space-y-3 pt-4",
							children: [
								/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, {
									htmlFor: "name",
									children: "Full name"
								}), /* @__PURE__ */ jsx(Input, {
									id: "name",
									value: fullName,
									onChange: (e) => setFullName(e.target.value)
								})] }),
								/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, {
									htmlFor: "email2",
									children: "Email"
								}), /* @__PURE__ */ jsx(Input, {
									id: "email2",
									type: "email",
									value: email,
									onChange: (e) => setEmail(e.target.value)
								})] }),
								/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, {
									htmlFor: "password2",
									children: "Password"
								}), /* @__PURE__ */ jsx(Input, {
									id: "password2",
									type: "password",
									value: password,
									onChange: (e) => setPassword(e.target.value)
								})] }),
								/* @__PURE__ */ jsx(Button, {
									className: "w-full",
									onClick: signUp,
									disabled: busy || !email || !password,
									children: busy ? "Creating…" : "Create account"
								})
							]
						})
					]
				})
			]
		})
	});
}
//#endregion
export { AuthPage as component };
